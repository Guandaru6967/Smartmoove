<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	   xmlns:xct="clr-namespace:Xamarin.CommunityToolkit.UI.Views;assembly=Xamarin.CommunityToolkit"
	   IsLightDismissEnabled="False"
	 Size="300,200"
             x:Class="Smartmoveapp.Views.Popups.TagAddPopup">

 <StackLayout>
  <Label FontSize="20"  TextColor="{StaticResource themecolorf}" Text="Add tag name" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontFamily="PoppinsBlack" > </Label>

  <Entry Text="{Binding TagText}"
	TextColor="{StaticResource themecolorf}"
                VerticalOptions="CenterAndExpand" 
	WidthRequest="200"
	BackgroundColor="{StaticResource themecolora}"
	FontFamily="PoppinsLight"
	FontSize="25"
                HorizontalOptions="CenterAndExpand" />
  <Grid ColumnDefinitions=".5*,.5*" Margin="10">
   <Button Grid.Column="0" Text="Add" Clicked="ReturnTagText"  FontFamily="PoppinsBlack"  FontSize="15" TextColor="{StaticResource themecolorf}" BorderColor="{StaticResource themecolorf}" BackgroundColor="{StaticResource themecolora}">
   </Button>
   <Button Grid.Column="1" Text="Cancel" Clicked="Cancel" FontFamily="PoppinsBlack" FontSize="15" BackgroundColor="{StaticResource themecolorf}" TextColor="{StaticResource themecolora}">
   </Button>
  </Grid>

 </StackLayout>
</xct:Popup>